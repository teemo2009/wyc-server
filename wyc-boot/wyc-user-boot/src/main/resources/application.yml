

spring:
  profiles:
    active: dev
  jmx:
    default-domain: wyc-admin
  datasource:
        type: com.alibaba.druid.pool.DruidDataSource
        druid:
          filters: stat
          driver-class-name: com.mysql.jdbc.Driver
          url: jdbc:mysql://${jdbc.url}:3306/wyc?useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true&useSSL=false
          username: ${jdbc.name}
          password: ${jdbc.password}
          initial-size: 1
          min-idle: 1
          max-active: 20
          max-wait: 60000
          time-between-eviction-runs-millis: 60000
          min-evictable-idle-time-millis: 300000
          validation-query: SELECT 'x'
          test-while-idle: true
          test-on-borrow: false
          test-on-return: false
          pool-prepared-statements: false
          max-pool-prepared-statement-per-connection-size: 20
  redis:
       port: 6379
       host: ${redis.url}
       password: ${redis.password}
       database: 1
       lettuce:
         pool:
           max-active: 8
           max-wait: -1ms
           max-idle: 500
           min-idle: 0




mybatis:
  mapper-locations: classpath*:mapper/*.xml

pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: countSql

  #JWT配置
jwt:
    ###TOKEN有效期 1小时 3600
    expire: 86400
    salt: wyc-admin&%^&
  #redis键值对配置
redis:
     prefix: 'WYC-USER:'
     user : '${redis.prefix}USER:'
     driver : '${redis.prefix}DRIVER:'
     manager: '${redis.prefix}MANAGER:'
     smsUser: '${redis.prefix}SMS:USER:'
     smsDriver: '${redis.prefix}SMS:DRIVER:'
     testCode: 6666
     orderList: 'WYC-COMMON:ORDERLIST'
     orderPush: 'WYC-COMMON:ORDERPUSH:'
     driverOrder: 'WYC-DRIVER:ORDER:'
     driverLocation: 'WYC-COMMON:DRIVER_LOCATION:'
     lastLocation: 'WYC-COMMON:DRIVER_LAST_LOCATION:'

server:
  port: 8072